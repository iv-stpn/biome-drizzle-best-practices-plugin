---
"biome-drizzle-best-practices-plugin": patch
---

Add `lint`/`lint:fix` scripts and a Husky `pre-commit` hook running lint, typecheck, and tests. Bump dev dependencies: `@biomejs/biome` to `>=2.5.4`, `biome-one-liner-plugin` to `1.4.0`, and `typescript` to `^7`. No changes to the published plugin.
